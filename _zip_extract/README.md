
  # TTS_CRM

  This is a code bundle for TTS_CRM. The original project is available at https://www.figma.com/design/GJErXh5QTqI7POLl3La0Gd/TTS_CRM.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  